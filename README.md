b4 running the goddamn program, u have 2 install flask and numpy for URL routing/page serving and multidimensional data structures, and mathematical formulas. get it?

pip install flask
pip install numpy
